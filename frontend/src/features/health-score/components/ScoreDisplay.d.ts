interface ScoreDisplayProps {
  score: number;
  maxScore?: number;
  label?: string;
}
export declare const ScoreDisplay: ({
  score,
  maxScore,
  label,
}: ScoreDisplayProps) => import("react").JSX.Element;
export {};
