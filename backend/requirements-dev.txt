# ── WealthWise AI — Dev/CI Tooling (Pinned for Reproducibility) ───────────────
# These EXACT versions must be installed both locally and in CI.
# Do not loosen — formatter behavior drifts across minor versions.

# ── Formatters & Linters ──────────────────────────────────────────────────────
black==24.10.0
isort==5.13.2
flake8==7.1.1
mypy==1.13.0

# ── Test Runners ──────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
aiosqlite==0.20.0
fakeredis==2.26.1

# ── Security ──────────────────────────────────────────────────────────────────
bandit==1.8.0
pip-audit==2.7.3
